settings:
  name: Search for passwords in code

KEYWORD:
- "EMAIL_HOST_PASSWORD ="
- "^password.*="
- "GITHUB_TOKEN:"

grep:
  assertStdout: False
  keyword:
  - [ "grep \"$(KEYWORD) \" * ./ -R --exclude='*.md' -I | cut -b -200" ]
