settings:
  name: Search for passwords in code

KEYWORD:
- "EMAIL_HOST_PASSWORD ="
- "^password.*="
- "GITHUB_TOKEN:"

grep:
  assertStdout: False
  keyword:
  - [ "grep \"$(KEYWORD) \" * ./ -R --exclude='*.md' -I | grep -v \"${{ secrets.GITHUB_TOKEN }}\"cut -b -200" ]
