settings:
    name: "Find Secrets with Semgrep"

install:
    assertReturnCode: 0
    semgrep:
    - [ python3 -m pip install semgrep ]

semgrep:
    assertStderrContains: 0 findings
    secrets:
    - [ semgrep --config "p/secrets" ]
