install:
  #npm:
  #- [ apt install -y nodejs npm ]
  package-lock-only:
  - [ "if [ -e .package-lock.json ]; then npm i --package-lock-only; fi" ]
  
npm:
  assertReturnCode: 0
  assertStdout: False
  audit:
  - [ /usr/bin/npm audit ]
  auditjson:
  - [ /usr/bin/npm audit -json ]
