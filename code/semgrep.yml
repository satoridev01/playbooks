settings:
  - name: "Static Source Code Analysis with Semgrep"
  - description: "Multiple files contain hardcoded secrets"
  - recommendation: "Remove the secrets from the code whenever possible"

install:
  - [ python3 -m pip install semgrep ]
  
semgrep:
  - [ semgrep --config auto ]
