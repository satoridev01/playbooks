settings:
  image: screetsec/sudomy:v1.2.1-dev
  name: Sudomy is a subdomain enumeration tool to collect subdomains and analyzing domains
  #example: satori run satori://dns/sudomy.yml --data='{"DOMAIN":"satori.ci"}' --output

sudomy:
  help:
    - sudomy -h
  run:
    - sudomy -d ${{DOMAIN}} --all 
