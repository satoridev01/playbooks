settings:
  name: "Ping the host and make sure there are no packet loss"
  
ping:
    assertStdoutContains: "0.0% packet loss"
    #HOSTS:
    #- www.satori-ci.com
    #- 3.16.7.14
    execute: 
    - [ ping -c $(HOSTS) ]
