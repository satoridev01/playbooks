tests:
    how-many-do-you-see:
        assertStdoutEqual: "2"
        input:
            - www.satori-ci.com
software:
    nmap:
        execute: 
            ["bash", "nmap [[tests]] -p 80,443 -oS oUTpuT >>/dev/null ; cat oUTpuT | grep '[oO0][pP][eE3][nN]' | wc -l | tr -d " "]  
