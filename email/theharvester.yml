settings:
  image: python
  name: E-mails, subdomains and names enumeration tool.
  #example: satori run satori://email/theharvester.yml --output

install:
  git:
    - apt install -qy git
  theharvest:
    - git clone --depth 1 https://github.com/laramies/theHarvester ; pip install -r theHarvester/requirements/base.txt

theHarvest:
  help:
    - python3 theHarvester/theHarvester.py -h
  run:
    - tbc
