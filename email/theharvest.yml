settings:
  image: kalilinux/kali-rolling
  name: E-mails, subdomains and names enumeration tool.
  #example: satori run satori://email/theharvest.yml --output

theHarvest:
  help:
    - theHarvester -h
  run:
    - tbc
