settings:
  #- rate: '5 minutes'
    cron:  '*/5 * * * ? *'
test:
    assertStdoutContains: "Freebee is changing the way people experience South Florida"
    curl:
      - [ curl -s "https://ridefreebee.com" ]
