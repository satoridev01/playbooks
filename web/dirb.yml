settings:
  name: "Dirb - web directory brute forcer"

install:
  assertReturnCode: 0
  dirb:
  - [ apt -y install dirb ]

dirb:
  assertStdout: False
  common:
  - [ dirb $(URL) /usr/share/dirb/wordlists/common.txt -S -r -i ]
