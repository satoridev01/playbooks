settings:
  image: alpinelinux/docker-cli
  name: A tiny web auditor with strong opinions.
  #example: satori run satori://web/twa.yml --data='{"HOST":"satori.ci"}' --output
  
install:
  twa:
    - docker build -t trailofbis/twa
  help:
    - docker run --rm -t trailofbits/twa -h
  run:
    - docker run --rm -t trailofbits/twa -vw ${{HOST}}
