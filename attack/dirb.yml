settings:
    labels: "web"
    priority: 1
    description: "Brute force web content in a URL"

install:
    - git clone https://salsa.debian.org/pkg-security-team/dirb.git
    - cd dirb/
    - bash configure
    - make

dirb:
    - ./dirb $(url) wordlists/common.txt
